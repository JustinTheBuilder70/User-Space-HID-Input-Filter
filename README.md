# User-Space-HID-Input-Filter

This program implements an HID input filter for Linux. It receives movement input from a mouse, applies an acceleration, and then sends the modified events to a virtual device that emulates the physical mouse.
